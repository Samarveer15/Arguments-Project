def shutdown(window button):
    F = input("Is the first step to click the window icon,true/false")
    if F == true :
        second_step (power button)

    else:
        print ("SORRY")

def second_step (power button):
    G = input("Is the second step to click the power icon,true/false")
    if G == true :
        third_step(Shutdown button):
    
    else:
        print("SORRY")

def thir_step (shutdown button):
    C = input("Is the third step to click the shutdown icon,true/false")
    if C == true :
        print("Shutdown") 
    
    else:
        print("Abort shutdown")